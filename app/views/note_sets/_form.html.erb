<%= form_for(@note_set) do |f| %>
  <% if @note_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@note_set.errors.count, "error") %> prohibited this note_set from being saved:</h2>

      <ul>
      <% @note_set.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
